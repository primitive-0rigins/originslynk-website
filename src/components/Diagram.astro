<figure class="workflow-figure" aria-labelledby="workflow-caption">
  <ol class="workflow-sequence">
    <li>
      <span class="icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 6h16v12H4zM4 7l8 6 8-6" /></svg></span>
      <span class="diagram-label">01 · Capture</span>
      <strong>Request arrives</strong>
      <span>Email, form, calendar, or document</span>
    </li>
    <li>
      <span class="icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M5 5h14v14H5zM9 9h6v6H9M12 2v3M12 19v3M2 12h3M19 12h3" /></svg></span>
      <span class="diagram-label">02 · Prepare</span>
      <strong>Defined work runs</strong>
      <span>Information is organized and drafted</span>
    </li>
    <li class="approval">
      <span class="icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M5 12.5l4 4L19 6.5" /></svg></span>
      <span class="diagram-label">03 · Review</span>
      <strong>You approve</strong>
      <span>Edit, approve, or stop the work</span>
    </li>
    <li>
      <span class="icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 18 20 5l-5 15-3-6-8 4zM12 14l8-9" /></svg></span>
      <span class="diagram-label">04 · Outcome</span>
      <strong>Work is ready</strong>
      <span>A draft, report, alert, or update</span>
    </li>
  </ol>
  <figcaption id="workflow-caption">Your existing tools remain the source of record. OriginsLynk prepares defined work; a person approves consequential results.</figcaption>
</figure>

<style>
  .workflow-figure { margin: 0; }
  .workflow-sequence { list-style: none; padding: 0; display: grid; gap: var(--space-5); grid-template-columns: 1fr; }
  .workflow-sequence li { min-width: 0; position: relative; padding: var(--space-5); }
  .workflow-sequence li:not(:last-child)::after { content: ""; position: absolute; left: 28px; top: calc(100% - 4px); height: calc(var(--space-5) + 8px); width: 1px; background: var(--diagram-line); }
  .icon { width: 48px; height: 48px; border-radius: 50%; border: 1px solid var(--rule-strong); background: var(--ground-raised); display: grid; place-items: center; margin-bottom: var(--space-4); }
  .icon svg { width: 22px; fill: none; stroke: var(--accent); stroke-width: 1.5; stroke-linecap: round; stroke-linejoin: round; }
  .approval .icon { background: var(--accent); border-color: var(--accent); }
  .approval .icon svg { stroke: #fff; }
  .workflow-sequence strong, .workflow-sequence > li > span:not(.icon) { display: block; }
  .workflow-sequence strong { margin-top: var(--space-2); font-size: var(--text-base); }
  .workflow-sequence > li > span:last-child { margin-top: var(--space-2); color: var(--ink-muted); font-size: var(--text-sm); }
  .diagram-label { color: var(--ink-faint); font-size: var(--text-xs); font-weight: 720; letter-spacing: var(--tracking-wide); text-transform: uppercase; }
  figcaption { margin-top: var(--space-6); text-align: center; color: var(--ink-muted); font-size: var(--text-sm); }
  @media (min-width: 48em) {
    .workflow-sequence { grid-template-columns: repeat(4, 1fr); gap: 0; }
    .workflow-sequence li { text-align: center; }
    .icon { margin-inline: auto; }
    .workflow-sequence li:not(:last-child)::after { left: calc(50% + 24px); top: 48px; width: calc(100% - 48px); height: 1px; }
  }
</style>
